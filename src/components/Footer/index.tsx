import * as styles from "./style.module.css";

const Footer = () => {
    return (
        <footer className={styles.footer}>
            <span>Words: X, Characters: X</span> <span>Ln X, Col X</span>
        </footer>
    );
};

export default Footer;
