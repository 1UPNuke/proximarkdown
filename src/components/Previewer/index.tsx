import * as styles from "./style.module.css";

const Previewer = () => {
    return (
        <output className={styles.previewer}>
            Preview
        </output>
    );
};

export default Previewer;
