import * as styles from "./style.module.css";

const Toolbar = () => {
    return (
        <header className={styles.toolbar}>
            <h1>Toolbar</h1>
        </header>
    );
};

export default Toolbar;
